%h1 ポスト一覧
= link_to "新規作成",new_post_path
%ul
  - @posts.each do |post|
    %li
      = post.id
      = post.content
      = link_to '詳細',post_path(post.id)
      = link_to '編集',edit_post_path(post.id)
      = link_to '削除', post_path(post), data: { turbo_method: :delete, turbo_confirm: "削除します、よろしいですか？" }

